<p id="notice"><%= notice %></p>

<h1>Listing Se Values</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @se_values.each do |se_value| %>
      <tr>
        <td><%= link_to 'Show', se_value %></td>
        <td><%= link_to 'Edit', edit_se_value_path(se_value) %></td>
        <td><%= link_to 'Destroy', se_value, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Se value', new_se_value_path %>
