<%= form_for(@surrogates_tag) do |f| %>
  <% if @surrogates_tag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@surrogates_tag.errors.count, "error") %> prohibited this surrogates_tag from being saved:</h2>

      <ul>
      <% @surrogates_tag.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
