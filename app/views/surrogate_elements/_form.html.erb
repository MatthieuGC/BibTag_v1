<%= form_for(@surrogate_element) do |f| %>
  <% if @surrogate_element.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@surrogate_element.errors.count, "error") %> prohibited this surrogate_element from being saved:</h2>

      <ul>
      <% @surrogate_element.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
